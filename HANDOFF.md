# 新商品開発ナビ ― 引き継ぎメモ

このメモは、別のClaudeアカウント（別セッション）が `git clone` でこのリポジトリを取得した後、
これまでの開発経緯や「なぜこの実装にしたか」を素早く把握できるようにするためのものです。
コミット履歴だけでは分からない設計判断・既知の制約をまとめています。

## リポジトリ構成

- `shinshouhin-navi.html` — アプリ本体。**リポジトリにHTMLはこの1ファイルだけです。**
  （以前ここに「`index.html` も同一内容で存在する」と書かれていましたが、
  2026-08-28に `git ls-files` で確認したところ**存在しませんでした**。
  二重管理は不要です。この1ファイルだけを編集してください。）
- 公開URL: https://taisei998.github.io/ec_ai/shinshouhin-navi.html
  （疎通確認済み。ユーザーはこのURLをブックマークして実データを入れています。
  **localStorageはオリジン単位なので、この固定URL以外で開くとデータが空に見えます。**
  検証用のローカルサーバーは必ず `http://127.0.0.1:8800` に固定すること。
  ポートを変えるたびに別の保管庫になり、ユーザーから「改修のたびにデータが初期化される」
  という問い合わせを実際に受けました。）
- **完全に単一HTMLファイルの静的アプリです。** バックエンドなし、ビルド工程なし。
  CSS/JSはすべてインライン、画像はbase64データURIで埋め込み。npm等の依存もありません。
  編集したら `index.html`（と、もし両方使うなら `shinshouhin-navi.html` も）を直接書き換えるだけです。

## 前提として知っておくこと

- このアプリはEC事業部の社内共有ツールで、Amazon・楽天市場・Yahoo!ショッピング・
  au PAYマーケット・Qoo10・カウシェの6PFを横断した新商品の規格（内容量・売価）検討を支援します。
- サーバーを持たないため、「ログイン」「データ保存」「毎朝の自動ニュース取得」はすべて
  ふつうのWebアプリとは違う工夫をしています。以下で個別に説明します。

## ログイン機能

- 共有パスワード方式（ユーザー名なし）。**パスワードの値はこのファイルには書きません**
  （このリポジトリは公開されているため）。値は `_ローカル専用（共有しない）/` を参照。
- 静的ファイルなので本物のセキュリティにはなりません（ソースを見れば分かる）。
  ユーザーにもその前提で合意を得た上で実装しています。
- ハッシュ関数は `crypto.subtle`（Web Crypto API）を**あえて使っていません**。
  最初はSHA-256で実装しましたが、`file://` オリジンやSafari等で `crypto.subtle` が
  使えないことがあり、原因不明のログイン不可バグになったため、依存のない同期関数
  **cyrb53**（`Math.imul`ベースの非暗号学的ハッシュ）に置き換えました。
  アプリ内の `STORED_HASH` にパスワードのcyrb53ハッシュ値を入れて照合しています。
  照合前に `toHalfWidth()` と `.trim()` を通すため、ハッシュを再計算するときも
  同じ正規化が必要です（`_ローカル専用（共有しない）/パスワード変更ツール.html` が同じ処理をします）。
- ログインは `<form>` の `submit` イベントに**依存していません**。サンドボックス化された
  プレビューiframe（`allow-forms` 権限なしのもの）では `submit` イベントが黙って握りつぶされ、
  「ボタンを押しても何も起きない」という再現しにくいバグになったため、`click`/`keydown(Enter)`
  の直接ハンドラに変更してあります。もしログインボタンが再び無反応になる不具合が出たら、
  まずこの点（何らかの形でformベースの実装に戻っていないか）を疑ってください。
- 全角/半角変換（IMEが全角のまま入力されるケースへの対処）も入っています。

## データモデル（`state` オブジェクト）

```
state = {
  ui: { theme, activeView, selectedNewsDate },
  aiInput: { name, volume, category, keyword, grade, platforms },
  aiResearch: null | {...},   // AIリサーチ結果（現在の作業スロット。1件のみ）
  aiResearchHistory: [        // AIリサーチの履歴（反映するたびに新規追加、上書きしない）
    { id, savedAt, name, volume, category, keyword, grade, data: {...元のaiResearch} }
  ],
  newsHistory: [],            // ニュース記録の配列（日付ごと、降順ソート）
  priceWatch: {                // 価格アラート機能（後述）。AIリサーチとは完全に独立。
    malls: [...],              // 監視対象モールキーの配列（設定画面で変更）
    items: [
      {
        id, name, volume, price, createdAt,
        keywords: [                          // 1商品は複数のアラートキーワードを持てる
          { id, keyword, mallUrls, history: [...] }   // mallUrls・historyはキーワードごとに独立
        ]
      }
    ]
  }
}
```

- **2026年8月26日まで、ブラウザの`localStorage`に状態データを一切保存していませんでした。**
  `markDirty()`（状態が変わるたびに呼ばれる関数）が `if (!sync.enabled) return;` から
  始まっており、**クラウド同期を設定していない端末では何も保存されず、タブを閉じる／
  再読み込みするとAIリサーチ履歴・ニュース履歴・価格アラートの登録商品が全部消える**
  という不具合があった（ユーザー報告により発覚）。「進捗を保存」ボタンでの手動
  エクスポート/インポートだけを永続化手段と想定していた設計の抜け穴。
  **修正済み**：`markDirty()`の冒頭で、`sync.enabled`に関わらず必ず
  `saveLocalState()`（1.5秒デバウンス）を呼ぶようにした。保存先は`localStorage`の
  `shinshouhin-navi.state`キー（クラウド同期の接続設定を保存する`shinshouhin-navi.sync`
  とは別）。起動時（`renderAll()`より前）に`loadLocalState()`で復元し、
  既存の`syncApplyRemote()`にそのまま渡している（クラウド同期のデータ受信処理を
  再利用。新しいマージロジックは書いていない）。クラウド同期が有効な端末では、
  起動後の`syncPull()`が`updatedAt`を比較して必要ならサーバー側の最新で上書きする
  ため、ローカル保存はあくまで「同期を設定していない場合の最終防衛線」という位置づけ。
  Playwrightで「商品登録→リロード→残っていることを確認」を検証済み。
- 例外だった`localStorage`の使い道（クラウド同期の接続設定＝URL・トークン）は今も
  別キー（`shinshouhin-navi.sync`）のまま変更していません
  （`syncSafeStore()` 関数、try/catchで囲んで環境非対応でも落ちないようにしてある）。
- `newsHistory` は元々単一の `state.news` でしたが、日付ごとの履歴管理に仕様変更した際、
  import処理内に**旧形式からの自動移行コード**を入れてあります（`state.news` が残っていたら
  `newsHistory` に取り込んで削除する処理）。古いエクスポートファイルを読み込んでも壊れません。

## AIリサーチ機能

- ユーザーが商品スペックを入力 → 依頼文をコピーしてClaudeに調査を依頼 → 返ってきたJSONを
  貼り付けて反映、という「貼り付け式」がメインの利用フロー（アプリ自体はWeb検索しません）。
- `recommendationOptions[]` に `regularPrice`（通常価格）/`salePrice`（SALE価格）/
  `couponPrice`（クーポン価格）の3段階価格。`regularPrice ≥ salePrice ≥ couponPrice` が前提。
- `platforms[]` に `priceMin`/`priceMax`/`priceRep`（代表価格）/`confidence`/`status`。
- **売れ行き確度判定（`computeSalesConfidence`）は `priceMin`〜`priceMax`の単純なレンジ内位置
  ではなく、`priceRep`（代表価格）からの乖離率で判定しています。** 最初はレンジ内位置で
  判定していましたが、レンジの振れ幅が大きいPF（出品者間の価格差が激しい等）で
  「確度：高」が出やすくなりすぎる問題があり、代表価格アンカー方式に変更した経緯があります。
  レンジが広い（最高値が最安値の2倍以上）場合はその旨を注記として自動で付けます。
- **Amazon・au PAYマーケット・Qoo10は自動調査が困難**（bot対策・JS描画のため）という
  前提でアプリが作られています。これらのPFが `priceMin`/`priceMax` 未取得、または
  `status: "unavailable"` の場合、レポートに「🛠 データの手動補完・追加取り込み」カードが
  自動で出ます。方法は2つ：
  1. 最安値/最高値/代表価格を数値で直接入力（`renderManualFillCard`）
  2. 該当PFのページをコピーしてClaudeに読み取らせる用の依頼文を生成し、返ってきた
     「そのPF1件分のJSON」を貼り付けて差し替える（`buildPfExtractPrompt`/`airPfPatchBtn`）
- **注意：Claude in Chrome（ブラウザ拡張）でこれらのPFを直接自動巡回させる案も検討しましたが、
  開発中は拡張が接続されておらず、実際に動かして検証はできていません。** 接続できる環境が
  あれば、Amazon/auPay/Qoo10を拡張経由で直接読みに行く機能を追加できる可能性があります。

## 履歴機能（トップバーの「📜 履歴」）

トップバーの「💬 フィードバック」の隣にある「📜 履歴」ボタンから、ニュース・AIリサーチ・
価格アラートの3つを横断して過去を振り返れるモーダル（`#historyOverlay`、使い方モーダルと
同じ`.fbOverlay`/`.fbCard`パターンを流用）を開ける。

- **ニュースタブ**（`renderHistoryNewsTab`）：既存の`state.newsHistory`の日付一覧を表示する
  だけの**ランチャー**。クリックすると`state.ui.selectedNewsDate`をセットしてモーダルを閉じ、
  ニュース画面へ遷移する（既存の日付タブ機能をそのまま使う。二重実装はしていない）。
- **AIリサーチタブ**（`renderHistoryAirResearchTab`）：`state.aiResearchHistory[]`を一覧表示。
  `state.aiResearch`（現在の作業スロット）自体は変更せず、結果を反映するたびに
  `arhAddEntry()`で**新規エントリーとして追加**する（同じ商品を再調査しても上書きしない。
  1日に複数商品を調べうるため、ニュースの「同日upsert」とは異なる設計）。「プレビュー」は
  `state`に依存しない`arhBuildPreviewHTML()`で表示、「この結果を読み込む」（`arhLoadEntry()`）
  で`state.aiInput`/`state.aiResearch`に復元してAIリサーチ画面へ遷移する。
- **価格アラートタブ**（`renderHistoryWatchTab`）：商品を選ぶ→**キーワードを選ぶ**→日付を選ぶ、
  の3段階ドリルダウン（1商品が複数キーワードを持てるようになったための構成）。判定表示は
  **モール別の判定バッジ・最安値のみ**（詳細な競合表までは出さない）。判定ロジックは新規実装せず、
  既存の`pwEvaluateMall(item, mallSnapshot)`に任意の過去スナップショット
  （`kw.history[n]`）をそのまま渡して再利用している。

`aiResearchHistory`は`syncBuildPayload`/`syncApplyRemote`・export/importの両方に、
`priceWatch`追加時と同じ「配列でなければ空配列にフォールバック」パターンで配線済み。

## 価格アラート機能（複数商品・モール横断の競合価格監視）

- 下部ナビの「価格アラート」から、**AIリサーチとは完全に独立した**複数商品の競合価格監視ができます。
  （旧バージョンでは「AIリサーチ結果の`competitors[]`に自社売価をぶつける」単一商品向けの
  プロトタイプでしたが、これは仕様変更により**丸ごと置き換え**ました。旧プロトタイプの
  `state.priceWatch = { prices: {} }` 形式のデータは非互換のため、古いエクスポートファイルを
  読み込んでも新形式の空状態にリセットされます（クラッシュはしません）。）
- 商品登録は4項目のみ：商品名・内容量・自社売価・**アラートキーワード**（＝モール内検索に
  使うキーワード）。監視対象モールは商品ごとではなく「設定」画面（`#pwMallChecks`）で
  アプリ全体として指定します。
- **1商品は複数のアラートキーワードを持てます**（`item.keywords[]`）。同じ商品を
  「天然水 500ml」「ナチュラルミネラルウォーター」のように複数の言い回しで同時に
  モール検索・監視したい、という要望から追加した仕様変更です。キーワードごとに
  `mallUrls`（検索URL上書き）と`history[]`（日別スナップショット）を独立して持ちます
  （＝キーワードを変えると別々の履歴タイムラインになる）。商品カードの末尾から
  キーワードを追加でき、キーワード文字列はシステム全体でユニークである必要があります
  （`pwFindKeywordByKeyword()`で重複チェック）。
  - **旧形式（`item.keyword`が単一文字列）からはデータを失わずに自動移行します**
    （`pwEnsureState()`内、1件だけのキーワードエントリーとして包み直す）。前回の
    priceWatch全面刷新時（旧`{prices:{}}`形式）は非互換でリセットしましたが、今回は
    ロスレスに変換できるため、既存の登録商品・履歴はそのまま引き継がれます。
- **判定は必ず4段階**（`PW_LEVEL_META`）：🔴競合が下回る／🟡同値で並走／🟢自社が最安／
  ○データ不足。競合データが無い場合は`nodata`を返し、誤って`lead`にしないよう
  `pwEvaluateMall()`内でガードしています。
- **内容量が違う場合は換算単価でも比較**（`pwParseUnitPrice()`）。「500ml×45本」のような
  容器×個数は個数あたり（円/本）、「5kg×2袋」のような重量×個数は合計重量あたり（円/kg）、
  単純な重量・容量表記はkg/Lあたりで算出します。自社・競合の単位ラベルが一致する場合のみ
  単価優先で比較し、一致しなければ生値比較にフォールバックします。
  - **競合の内容量欄が空欄でも、商品名から同じパターンで抽出を試みます**
    （`pwCompetitorUnitPrice()`、2026年8月28日〜）。AIに調査を依頼すると「内容量（分かれば）」
    は任意項目のため空欄で返ってくることがありますが、Amazon等の商品名にはそのまま
    「500ml×24本」のような表記が入っていることが多く、`pwParseUnitPrice()`は文字列内を
    正規表現で探すだけなので追加の解析ロジックを書かずに`c.name`をそのまま渡すだけで
    動きます（既存関数の再利用）。実例：**生値では自社より安い競合でも、内容量欄が
    埋まっていないと「本数が少ないから実は1本あたりは高い」ことが判定に反映されず、
    誤って「競合が下回っている」と表示され続けるバグがありました**（ユーザー報告：
    「水の売価では負けているけど、1本単価では勝っている」）。名称から推定した場合は
    UI上に「・名称から推定」（モール別サマリー）／「推定」タグ（自社を下回る競合リスト、
    `.pwAdTag`を再利用）を必ず表示し、AIが実際に確認した値と区別できるようにしています。
- **商品全体の判定は、全キーワード×全モールの中から最も注意度の高いものを採用**
  （`pwAggregateItemLevel()` → `pwEvaluateItemAll()`、優先順位 alert > tie > nodata > lead）。
  この優先順位は仕様書に明記されていなかったための実装判断です。**「データ不足」は
  「自社が最安」より注意度が高い扱い**なので、一部のモール／キーワードしかデータが
  揃っていない商品は、他が良好でも商品全体としては「データ不足」表示になります
  （意図した挙動）。
- **前日比・新規競合検知**（`pwDiffAgainstPrevious(kw, mallKey)`）は、キーワードごとの
  `history[]`（日付キーでupsert、ニュース機能と同じパターン）の最新2件を競合URL
  （無ければ商品名+ショップ名）で突き合わせて計算します。判定結果は保存せず、描画の
  たびに`history`から再計算します。

### 画面構成：「①商品を登録」「②商品を見る」の2タブ（2026年8月28日〜）

以前は1画面に「モール調査カード→登録フォーム→登録済み商品の全カード縦並び」が
すべて詰まっており、商品数が増えるとスクロールが長くなって見づらかったため、
ユーザー依頼で2タブに分割しました。

- `#pwTabs`（`.pwTab`ボタン）で`state.ui.activeWatchTab`（`"register"|"view"`、既定`"view"`）
  を切り替え、`#pwRegisterSection`/`#pwViewSection`の`hidden`を出し分けます
  （ニュース機能の`.newsCategoryTab`と同じ考え方の独立実装。無関係なニュース機能の
  クラス名を巻き込まないよう、あえて別クラス`.pwTab`にしています）。
- 「①商品を登録」タブ：登録フォーム（`#pwRegisterCard`）のみ。**登録後もこのタブに
  留まります**（連続登録がしやすいよう現状維持）。
- 「②商品を見る」タブ：**モール調査カード（`#pwCheckCard`）はここに移動しました**
  （既存キーワードのデータ更新操作であり、未登録の商品には使えないため、登録専用の
  ①タブには置けない、という理由でユーザー確認の上こちらに配置）。続けて商品一覧
  ピッカー（`renderPwProductPicker()` → `#pwProductPicker`、商品名＋判定バッジの
  コンパクト行）と、選択中1商品だけの詳細カード（`renderPwProductDetail()` →
  `#pwProductDetail`、中身は既存の`pwRenderItemCard()`をそのまま再利用）を表示する
  「一覧→選択→詳細」ドリルダウンにしました（履歴モーダルの`renderHistoryWatchTab()`と
  同じ考え方をメイン画面にも導入）。
  - 選択中の商品IDは`state.ui.selectedWatchItemId`。サマリーバーの判定チップ
    （`pwActiveFilter`）で一覧を絞り込んだ結果、選択中の商品が対象外になった場合は
    表示中の先頭商品へ自動フォールバックします。
  - 商品が0件のときの空状態カードには「①登録タブへ」ボタンを追加しました
    （画面が分かれたことで、フォームが同じ画面に見えなくなったための追従修正）。
  - `state.ui`は他のUI状態（`activeNewsCategory`等）と同様、localStorage自動保存の
    対象外（`syncBuildPayload()`が持ち出す項目に含まれない仕様）なので、リロード後は
    タブ・選択が既定値に戻ります。これは既存の挙動を踏襲したもので、今回新たに
    導入した制約ではありません。

### 「②商品を見る」タブの拡張：2階層検索セレクター・キーワードON/OFF・自動化プロンプト（2026年8月28日〜）

商品・キーワードが増えて上記のピッカーだけでは探しにくくなったため、ユーザー依頼で
さらに次の4点を追加した。

- **2階層のコンボボックス型セレクター**（ユーザーが手書きの画面イメージを提示し、
  それに合わせて実装）：`#pwSelectCard`内の商品コンボボックス（`#pwProductCombo`、
  `#pwProductSearchInput`＋▽ボタン＋開閉する`#pwProductComboList`）で商品を検索/
  一覧から選択→選ぶと`#pwProductDetail`内にキーワードコンボボックス
  （`#pwKeywordCombo`、`[data-role='kwsearch']`）が現れ、同様に検索/一覧から
  キーワードを選択→**「🔍 検索」ボタン（`[data-act='watchSearch']`）を押すまで
  モール別データは表示されない**（`pwWatchResultShown`フラグで制御。商品・キーワード
  の選択を変えると自動的にfalseに戻り、再度「検索」を押すまでプレースホルダー表示に
  戻る）。キーワードコンボの各行にはON/OFFトグル（🔔/🔕）も付いており、クリックしても
  一覧が閉じないよう開閉状態を`pwKeywordComboOpen`で保持している。
  各コンボボックスの検索input自体は「▽ボタンや自身のfocusで一覧を開く／入力で
  ライブフィルタ／選択すると値が確定してinputの表示に反映され一覧が閉じる／外側
  クリックで閉じて選択中の値の表示に戻る」という標準的な挙動。商品側のinputは静的
  HTML（`renderPwAll()`では作り直さない）、キーワード側のinputは商品カードごと
  再構築されるが、**どちらも「1文字打つごとの絞り込み」は一覧コンテナ
  （`#pwProductComboList`/`#pwKeywordComboList`）だけを差し替える専用関数
  （`renderPwProductComboList()`/`renderPwKeywordComboList()`）を使い、input要素
  自体は触らないことでタイピング中にフォーカスが飛ぶのを防いでいる**（商品選択・
  キーワード選択・ON/OFF切替のようなクリック操作は`renderPwAll()`の全体再描画で
  よい＝タイピング中でなければフォーカス喪失は問題にならないため）。
- **キーワード単位のアラートON/OFF**（`kw.alertEnabled`、既定`true`）：
  `pwToggleKeywordAlert()`で反転。OFFのキーワードは`pwEvaluateItemAll()`
  （集計の一本道）と`pwBuildResearchPrompt()`（依頼文の対象商品列挙）の**両方から
  除外**する（サマリーバー件数・商品全体の判定バッジ・依頼文の対象キーワード一覧に
  反映）。ただし`pwEvaluateKeywordMalls()`（1キーワード分のモール別データ取得）は
  素通しのままなので、**OFFにしてもキーワード行をクリックすればモール別データ自体は
  引き続き見られる**（「一時的にアラート対象から外す」であって「データを消す」
  ではない、という意図的な区別）。
- **「依頼文を作成」に自動化セットアップの指示を追記**：`pwBuildPrompt()`を
  `pwBuildResearchPrompt()`（調査依頼本体）＋`pwBuildScheduleAddendum()`
  （自動化セットアップの指示）に分割。生成される依頼文の末尾に、「毎日10:00(JST)
  ＝cron `0 1 * * *`（UTC同日01:00）で今回と同じ調査を行うルーティンを作成/更新
  してほしい」という指示を追記している。ルーティンのプロンプト自体を裏で
  組み立てる処理は書かず、「このメッセージの、この指示より前の部分をそのまま使って」
  と自然文で指示するだけに留めている（実装コストを増やさないための判断）。
  実際のルーティン作成は、この依頼文をユーザーがClaudeとの会話に貼り付けた
  タイミングでClaude側が行うものであり、アプリ側からRemoteTriggerを直接
  操作することはない。
- **レイアウト整理**：「調査結果のJSONを貼り付け」欄（`#pwApplyCard`）を
  `#pwCheckCard`の中から切り出し、2階層セレクター（`#pwProductPicker`/
  `#pwProductDetail`）の下に移動した。また`#pwCheckCard`にベタ書きされていた
  長い説明文は、ℹ️アイコン（`#pwCheckInfoBtn`）をクリックすると開く吹き出し
  （`.pwInfoPopover`、`position:absolute`）に変更し、常時表示ではなくした
  （外側クリックで閉じる。既存のグラフ用`#tooltip`はhover専用でtap向けでは
  なかったため、この吹き出しは新規実装）。
- **既存バグの修正**：`#importFile`読み込み時の`state.ui`フォールバックが
  `activeWatchTab`/`selectedWatchItemId`を復元していなかった（前回実装時の
  見落とし）。今回`selectedWatchKeywordId`を追加するタイミングで、3フィールドとも
  個別に`typeof ... === "undefined"`でチェックする形に直した。

### 「①商品を登録」タブ：登録済みマスタからのコピー／編集（2026年8月28日〜）

商品・キーワードのバリエーションが増えると、似たような内容を毎回ゼロから手入力するのが
手間になるため、`#pwRegisterCard`（登録フォーム）の下に`#pwMasterCard`
（登録済みマスタ一覧）を追加した。

- **商品名→（内容量・キーワード）の2階層ドリルダウン**：`pwMasterAllNames()`が
  `priceWatch.items`から重複のない商品名一覧を作り、`renderPwMasterNames()`が
  `#pwMasterSearchInput`（部分一致・ライブフィルタ）＋一覧として表示する。商品名の
  行をクリックすると`pwMasterEntriesForName(name)`（同じ名前を持つ全item×keywordの
  組を平坦化）を`renderPwMasterEntries()`が一覧表示し、検索欄の役割も
  「内容量・キーワードで検索」に切り替わる（`←戻る`ボタン`#pwMasterBackBtn`で
  商品名一覧に戻れる）。「見る」タブの2階層コンボボックスと違い、こちらは
  データ取得を伴わないため検索結果は即時反映（「検索」ボタンでの明示的な確定操作は
  不要）。検索inputは静的HTMLで、絞り込みは`#pwMasterRows`の中身だけを差し替える
  ため、ここでもタイピング中のフォーカス喪失は起きない。
- **コピー／編集ボタン**：内容量・キーワードの各行に「📋 コピー」「✏️ 編集」を
  用意し、どちらも`pwMasterLoadToForm(itemId, kwId, isEdit)`が上の登録フォーム
  （商品名・内容量・自社売価・アラートキーワード・モール別URL）に値を読み込む。
  - **コピー**は`pwEditTarget`を`null`のままにする＝そのまま「＋ 商品を登録」を
    押すと**新規の別アイテムとして追加**される（似た商品を素早く複製登録したい
    ユースケース）。キーワードを変えずに送信すると、既存の`pwFindKeywordByKeyword`
    の重複チェックにそのまま引っかかる（意図した挙動：キーワードや内容量を変えて
    から登録し直す運びになる）。
  - **編集**は`pwEditTarget = {itemId, kwId}`をセットし、フォームの送信ボタンの
    表示を「✏️ 変更を保存」に変える（`pwUpdateFormModeUI()`）。この状態で送信すると
    `pwAddItem`ではなく`pwUpdateItemKeyword(itemId, kwId, fields)`が呼ばれ、
    **新規アイテムを作らず既存のitem（name/volume/price）とそのキーワードのテキスト・
    モールURLを上書き**する。重複チェックは、見つかった一致が「今まさに編集中の
    そのキーワード自身」である場合は許容するよう`pwEditTarget`で例外にしている
    （変更せずに保存し直せるようにするため）。「編集をキャンセル」
    （`#pwCancelEditBtn`）でいつでも通常の新規登録モードに戻せる。
  - item内の他のキーワードは編集対象に含まれない。ただし`name`/`volume`/`price`は
    item単位で共有のフィールドなので、そのitemの**他のキーワードにも表示上反映
    される**（1つのitemに複数キーワードがある場合の当然の帰結であり、意図した
    挙動）。

### 子ASINの登録・ASIN基準でのマスタ選択・1キーワード単位の依頼文（2026年8月28日〜）

実際に運用してみて、①Amazonの子ASINを商品情報として持たせたい、②登録済みマスタは
ASINを軸に探したい、③「依頼文を作成」が全商品・全キーワードを一括りにした1つの
巨大な依頼文になっており、AIの応答が長大になりすぎてJSONが生成途中で切れる
（`Unterminated string in JSON`エラー）事故が実際に発生した、という3点の指摘を受けて
対応した。

- **`item.asin`フィールドを追加**（`pwEnsureState()`の移行ループで未定義なら`""`に
  正規化）。登録フォーム（`#pwFormAsin`、「子ASIN（任意・Amazon）」）で入力し、
  `pwAddItem`/`pwUpdateItemKeyword`の両方に配線済み。**必須にはしていない**
  （Amazon以外のモールしか調べない商品や、既存の登録データとの後方互換のため）。
- **登録済みマスタ一覧をASIN基準に変更**：`pwMasterAllAsins()`が`item.asin`で
  グルーピングし（`pwMasterEntriesForName`→`pwMasterEntriesForAsin`、
  `pwMasterSelectedName`→`pwMasterSelectedAsin`にリネーム）、ASIN未登録の商品は
  空文字キーの1グループにまとめて一覧の最後に「（ASIN未登録）」として表示する
  （既存データを迷子にしないための後方互換対応）。検索はASIN・商品名の両方に
  部分一致するので、ASINを覚えていなくても商品名から辿れる。レベル2（内容量・
  キーワード一覧）には商品名も併記し、ASINだけでは分かりにくい商品を識別できる
  ようにした。
- **「商品を見る」の商品コンボボックスもASINで検索できるように**：
  `renderPwProductComboList()`のフィルタ条件に`it.asin`を追加し、一覧の各行にも
  ASINを小さく併記（`.pwProductRowAsin`）。確定後にinputへ表示する値は引き続き
  商品名（ASINは検索・識別補助という位置づけ）。
- **1商品・1キーワード単位の依頼文を追加**：これまでの「📝 依頼文を作成」
  （`pwBuildPrompt()`、全商品・全有効キーワードを一括で列挙＋10時自動化の追記付き）は
  そのまま残しつつ、「見る」タブでキーワードを選択すると出てくる「🔍 検索」ボタンの
  隣に「📝 このキーワードだけの依頼文を作成」を新設した。クリックすると
  `pwBuildSingleResearchPrompt(item, kw)`が選択中の1商品×1キーワードだけの依頼文
  （子ASINが登録されていれば商品情報に含める）を生成し、`#pwPromptCard`にスクロール
  して表示する。**こちらには10時自動化の追記は付けていない**（1キーワードずつ手動で
  都度確認する用途を想定しており、自動化は引き続き一括の「依頼文を作成」側の役割と
  整理した）。一括版と使い分けられるよう両方残す方針とした（一括を完全に置き換える
  という指示ではなく「1つずつ調べられるようにしたい」という要望だったため）。

### 「商品を見る」のキーワード選択をコンボボックス→タブ切替に変更（2026年8月28日〜）

コンボボックス＋「🔍 検索」ボタンでの1キーワードずつの確認は、複数キーワードを
登録している1つのASINについて「競合に価格が下回られていないか」をまとめてサッと
見比べたい、という実際の使い方には手間が多すぎた（キーワードを選ぶ→検索を押す、を
キーワードの数だけ繰り返す必要があった）という指摘を受けて、キーワードの切り替えを
タブ形式に変更した。

- `pwRenderItemCard(it)`内の「キーワードを選択」コンボボックス（`#pwKeywordCombo`/
  `#pwKeywordComboList`、検索input・開閉ロジック一式）を撤去し、「🔖 登録キーワード」
  という見出し＋`#pwKeywordTabs`（`renderPwKeywordTabs(it)`が描画）に置き換えた。
  各タブは`pwBuildKeywordTabHtml(it, kw)`が生成し、キーワード文言の前に判定レベルの
  色ドット（`.pwKeywordTabDot` + `PW_DOT_CLASS`、alert=赤/tie=黄/lead=緑/nodata=灰）を
  表示するので、**タブを開かずに一覧を見るだけでどのキーワードで競合に下回られて
  いるか一目で分かる**。クリックすると即座に（検索ボタンを挟まず）そのキーワードの
  モール別詳細が下の`#pwKeywordFocusArea`に切り替わる。
- **「🔍 検索」ボタンと`pwWatchResultShown`ゲートは撤去**した。以前は手書きの
  ワイヤーフレームに合わせて「検索を押すまでデータを隠す」設計にしていたが、
  タブ切替はそもそも「既に取得済みのローカルデータをどう見るか」の話であり
  ネットワーク越しの実際の検索を伴わないため、都度ボタンを押す一手間はもはや
  合理性がない（今回の要望を機にシンプル化した）。
- 商品を選択すると**最初のキーワードのタブが自動選択**される（以前のコンボボックス
  版は「明示的に選ぶまで何も選択しない」方針だったが、タブ即時切替の下では
  「選んだら即なにか見える」方が自然なため方針を変えた）。アラートON/OFFトグルは
  タブ自体からは外し、キーワード詳細エリア（`#pwKeywordFocusArea`）内の
  「キーワードを削除」ボタンの隣に移設した。
- **「📝 このASINの全キーワード分の依頼文を作成」を新設**（`pwBuildProductResearchPrompt(item)`）。
  「全商品まとめて」（truncationの原因になりやすい）と「1キーワードだけ」の
  中間の粒度として、選択中の商品（ASIN）に登録されている有効な全キーワードを
  1つの依頼文にまとめる。1発の調査でそのASINの競合価格変動を全キーワード分
  まとめて確認したい、という要望に応えるもの。既存の「📝 この1キーワードだけの
  依頼文を作成」（`pwBuildSingleResearchPrompt`）はそのまま残しており、
  ASIN単位・キーワード単位・全商品一括の3段階の粒度を使い分けられる。
- 添付されたスクリーンショットの指摘（`pwItemFacts`の並びに「登録キーワード」の
  欄が無かった）に対応する形で、上記のタブ見出しが「🔖 登録キーワード」欄として
  機能する。

### 「商品を選択」を①一番上に移動、②タブ選択方式に変更（2026年8月28日〜）

手書きの画面イメージを提示され、「商品選択は『商品をみる』の一番上で行い、ASIN別で
見れるようにしてほしい」という依頼を受けて対応した。指摘の趣旨は、直前に導入した
「登録キーワード」タブ切替（コンボボックスより即座に切り替えられる）と同じ考え方を
商品レベルの選択にも適用してほしい、というもの。

- **`#pwSelectCard`（商品選択）を`#pwViewSection`の一番上に移動**した
  （`#pwEmptyCard`の直後、`#pwCheckCard`「🕘 モール調査」より前）。以前は
  モール調査カードが先に表示されていたが、商品を選ぶことが最初の行動になるよう
  並び順を変えた。
- **コンボボックス（検索input＋▽ボタン＋開閉するドロップダウンリスト）を撤去し、
  「登録キーワード」タブと同じ「検索input＋常時表示のタブバー」方式に統一**した。
  `renderPwProductTabs()`が`#pwProductTabs`にASIN・商品名でライブフィルタされた
  商品を`.pwProductTab`ボタンとして並べる（`pwBuildProductTabHtml(it)`、判定レベルの
  色ドット付き。キーワードタブの`.pwKeywordTabDot`は`.pwTabDot`という共通クラス名に
  リネームし、商品タブ・キーワードタブの両方で使い回している）。開く/閉じるという
  概念自体が無くなったので、`pwSyncProductComboInput()`（閉じたときに選択中の値を
  inputに反映する処理）や外側クリックでの閉じる処理も丸ごと不要になり削除した。
  検索inputは「フィルタ用の入力欄」に徹し、選択状態はタブの`.active`表示だけで示す。
- 検索は既存通りASIN・商品名の両方に部分一致する。商品を1件クリックすると
  検索文字列は自動でクリアされ、次にまた全商品がタブで一覧表示される状態に戻る
  （選んだあとに毎回手動でクリアする手間を無くすため）。

> ⚠ 上記の「商品名のタブバー」は下記の通り**さらに変更済み**（キーワードのバーに置換）。
> 履歴として残すが、現行の挙動は次項を参照。

### 同一ASINの重複登録バグ修正・検索欄の下を「登録キーワード」バーに（2026年8月28日〜）

ユーザーから2点の指摘を受けて修正した。

**① 同じASINで2回登録すると商品が2件に分かれるバグ（データモデルの不具合）**

`pwAddItem()`がASINを一切見ずに常に`pw.items.push()`していたため、同じASINで
キーワードを2つ登録すると「同じ商品名のアイテムが2件、それぞれキーワード1つずつ」
という状態になっていた。これが「商品バーに同じ商品名が2つ並ぶ」「登録キーワードが
1つしか出ない」という2つの症状の**共通の根本原因**だった。

- **書き込み側の修正**：`pwAddItem()`は、入力されたASINが既存アイテムと一致する場合は
  新規アイテムを作らず、そのアイテムの`keywords[]`に追加する（＝1ASIN＝1商品に複数の
  監視キーワードがぶら下がる形）。商品共通項目（商品名・内容量・自社売価）は最新の
  入力で上書きする（同じASINなら同じ商品なので、内容量・売価の変更を兼ねた再登録として
  扱うのが自然）。**ASINが空の登録は突き合わせようがないので従来どおり常に新規追加**。
- **既存データの移行**：`pwEnsureState()`に、同じ非空ASINを持つアイテムを1件へ統合する
  処理を追加（先に現れたアイテムを残し、後続のキーワードをそこへ移すのでhistoryを含め
  データは失われない）。統合が発生したときだけ`markDirty()`する。移行フラグを持たせて
  いないのは、統合後は重複が存在しないので毎回走っても何も起きない（＝冪等）ため。

**② 検索欄の下のバーを「商品名」から「登録キーワード」に**

ユーザー確認の結果、**ASINの選択は検索欄だけで行い**（`renderPwProductPicker()`が
`pwSearchMatchedItems()`の先頭を自動選択）、**検索欄の下のバーは常に選択中ASINの
「🔖 登録キーワード」だけ**にした。商品名のタブバー（`renderPwProductTabs`/
`pwBuildProductTabHtml`/`.pwProductTab`）は撤去。

- 選択中の商品は検索欄の直下に「ASINコード＋商品名」（`#pwSelectedAsin`、
  `.pwSelectedAsinCode`はモノスペースのバッジ表示）として表示する。ASIN未登録の
  商品は「ASIN未登録」と表示。
- `#pwProductMatchHint`で該当件数を案内する：複数該当なら「N件が該当します。ASINを
  入力すると1件に絞り込めます。」、0件なら「該当する商品がありません（選択中の商品を
  表示しています）。」。**0件のときは選択を変えない**——打ち間違いの途中で選択中の
  商品が消えて画面が空になるのを防ぐため。
- **キーワードバーは`#pwSelectCard`（商品カードの外）に1本だけ**置き、
  商品カード内にあった重複するタブバーは削除した（ユーザー確認の上「上のバーに
  一本化」）。商品カードに残るのは「このASINに新しいアラートキーワードを追加」欄と
  2つの依頼文ボタン。`#pwKeywordTabs`はカードの外にあるので`pwWireItemCards()`では
  触らず、`renderPwProductPicker()`側で描画する（`renderPwAll()`内の呼び出し順で
  ピッカー→詳細の順になっており、キーワードの自動フォールバックが先に確定する）。

### 「自社を下回る競合」を最大5件リスト表示（2026年8月28日〜）

従来はモール別ブロックに「最安 ¥1,287」しか出ておらず、**誰がいくらで下回って
いるのか**が分からなかった。ユーザー要望で、判定が🔴のとき自社売価を下回っている
競合の商品名と売価を最大5件表示するようにした。

- **新規`pwUndercuttingCompetitors(item, mallSnapshot, limit)`**：
  **`pwEvaluateMall`と全く同じ比較モードを使う**のが要点。自社と単位が揃う換算単価を
  出せる競合が1件でもあれば「単価モード」、無ければ「生値モード」に切り替える。
  こうしないと**判定バッジは🔴なのにリストが空**（あるいは逆）という食い違いが起きる。
  単価モードでは単位を揃えられない競合は対象外——これも`pwEvaluateMall`の判定と同じ扱い。
  返り値は`{total, rows}`で、`total`は上限適用前の件数（「ほかN件」表示に使う）。
- **並び順は「下回り幅の大きい順」**（＝実質的に安い順）。単価モードでは単価差、
  生値モードでは金額差でソートする。実測例：自社80.0円/本のとき、生価格が最安の
  ¥1,498（Sohten・24本＝62.4円/本）は**4位**に落ち、¥2,141（BIYOUDO・42本＝51.0円/本）が
  1位になる。**生価格の安さと単価の安さが逆転するケースを正しく扱えている。**
- **新規`pwBuildUndercutListHtml(item, mallSnapshot)`**：`.pwUnderList`として
  モール別ブロックの末尾に描画。1行＝商品名（URLがあればAmazon/楽天へのリンク、
  広告枠は`.pwAdTag`で「広告」表示）＋ショップ名＋価格（単価モードなら換算単価も）
  ＋下回り幅。5件を超える場合は「ほか N件が自社を下回っています」を出すので、
  **上限で切り捨てたことが黙って隠れない**（silent truncationを避ける）。
- 判定が🟢/🟡/○のときはリスト自体を出さない（下回っている競合が0件なので空になる）。
- ベンチマーク側には追加していない：ベンチマークは1行＝1商品なので、下回っていれば
  その商品名と価格が既に表示されている（重複になる）。

### ベンチマーク商品（競合ASINの定点観測）（2026年8月28日〜）

キーワード検索方式は「検索結果1ページ目の顔ぶれが毎回入れ替わる」ため、
`pwDiffAgainstPrevious()`の「前回比」が実質「別の商品との比較」になりがちで、
特定の競合の値動きを追えなかった。ユーザー要望で、**競合の特定Amazon子ASINを
定点観測する「ベンチマーク商品」**を追加した。

- **データモデル**：`item.benchmarks[]` = `{id, asin, label, alertEnabled, history[]}`。
  `pwEnsureState()`で既定化するので、`benchmarks`を持たない既存データは0件として
  扱われる（後方互換）。同一ASINのitem統合ロジックでもbenchmarksを引き継ぐ。
- **★設計の核：既存3関数を無改修で再利用している**。`history`を**キーワードと
  完全に同じ形**（`{date, checkedAt, malls:{amazon:{status,note,competitors:[...]}}}`）
  にし、**追跡している1商品を1要素の`competitors`配列に包んで**保存することで、
  `pwUpsertHistory()`・`pwDiffAgainstPrevious()`・`pwEvaluateMall()`がそのまま動く。
  結果として**換算単価比較（`pwParseUnitPrice`）と4段階判定が無料で手に入った**。
  実際に「自社500ml×24本 ¥3,264（136円/本）」vs「競合500ml×48本 ¥4,488（93.5円/本）」で
  **生価格は競合の方が高いのに単価では負けている**ケースを正しく🔴と判定できている。
  新設した`pwEvaluateBenchmark(item, bm)`は`pwEvaluateMall`を呼ぶだけの薄いラッパー。
- **Amazon限定**（`PW_BENCHMARK_MALL = "amazon"`）：ユーザー確認の上、楽天の定点観測は
  従来のキーワード検索でカバーする方針にした。ASINはAmazon固有の識別子であり、
  楽天は商品URLという別の識別子になるため、同じ`asin`フィールドで両対応すると
  モデルが濁る。必要になったらモール別の識別子フィールドを足す方が素直。
- **集計への反映**：`pwEvaluateItemAll()`にベンチマークのループを1つ足しただけで、
  `pwAggregateItemLevel`・`pwGlobalLowestPrice`・`renderPwSummaryBar`のすべてに
  伝播する（呼び出し側は無改修）。`alertEnabled === false`はキーワードと同様に
  集計・依頼文の両方から外れるが、**行のデータ自体は引き続き閲覧できる**。
- **UI**：`#pwKeywordTabs`の末尾に「🎯 ベンチマーク (N)」タブを**常に**追加
  （0件でもそこから登録できるように）。`state.ui.watchDetailMode`
  （`"keyword"|"benchmark"`）で`#pwKeywordFocusArea`の中身を出し分ける。
  **キーワードが0件の商品では自動的にbenchmarkモードに寄せる**（見るものが
  ベンチマークしかないため）。ASIN入力は**Amazonの商品URLを貼ってもASINを抽出**
  （`pwNormalizeAsin`）、英数10桁でない入力・自社商品自身のASIN・同一item内の
  重複はエラーにする。
- **「ギラギラ」演出**（ユーザーの明示的な要望）：ベンチマークに自社売価を
  下回られている状態を`.pwGlowAlert`（赤いリングが脈打つCSSアニメーション）で
  **①該当行 ②🎯タブ ③商品カードのヘッダーバッジ**の3ヶ所に付ける。
  🎯タブも光らせるのは、**タブを開かずに気付けるようにする**ため。
  常時点滅はアクセシビリティ上の負荷になりうるので`prefers-reduced-motion`を
  尊重し、動きを止めた場合も赤リングは残して視認性を落とさない。
- **依頼文**：ユーザー確認の上、専用ボタンは作らず**既存の「このASINの全キーワード分の
  依頼文」と一括版の両方にベンチマークのセクションを含める**
  （`pwBuildBenchmarkPromptLines()`）。「この1キーワードだけの依頼文」には
  **含めない**（趣旨が1キーワードに絞ることなので）。JSONスキーマには`results`と
  並ぶ`benchmarks`配列を追加し、`pwApplyResultJson`のガードを
  **「`results`か`benchmarks`のどちらかが配列ならOK」に緩めた**ので、
  旧スキーマ（`results`のみ）のJSONもそのまま通る。
- **⚠ 実務上の制約（誠実な期待値）**：Amazonの商品ページも検索結果ページと同様に
  bot対策の対象で、**取得できないことがある**。Claude in Chromeを使った対話フロー
  （依頼文をこの会話に貼る運用）では取得可能だが、ヘッドレスなクラウドルーティンでは
  `unavailable`になりやすい。「ベンチマークにすれば必ず取れる」わけではなく、
  **取得できたときの前回比の信頼性が上がる**という改善。
- **スコープ外**：履歴モーダルの価格アラートタブ（`renderHistoryWatchTab`）は
  `item.keywords`だけを走査しているので、**壊れないがベンチマークは出ない**。
  レベル2でキーワード行とベンチマーク行を区別するUIが必要になり今回の要望の範囲を
  超えるため、意図的に触っていない。
- **既知の仕様**：`state.ui`は`syncBuildPayload()`の対象外なので、
  `watchDetailMode`もリロードで既定値（`"keyword"`）に戻る。これは
  `activeNewsCategory`等の既存UI状態と同じ挙動で、今回導入した制約ではない。
- **`pwGlobalLowestPrice`は生価格の最小値**を返す（従来通り）。単価では負けている
  ベンチマークがあっても「競合最安値」欄には生価格の最安が出るため、単価ベースの
  優劣は判定バッジ側で見ることになる。これも従来のキーワード側と同じ挙動。

### 調査対象モールをAmazon・楽天市場の2モールに縮小（2026年8月28日〜）

Yahoo!ショッピング・au PAYマーケット・Qoo10・カウシェは元々「bot対策・JS描画で自動調査
が難しく、データ不足になりやすい」と各所のヒント文で注記していた通り実運用に乗らず、
ユーザーからの明示的な依頼で価格アラートの調査対象をAmazon・楽天市場の2モールだけに
絞った。

- `PW_DEFAULT_MALLS = ["amazon", "rakuten"]`を新設し、`pwEnsureState()`の
  `state.priceWatch.malls`の既定値をこれに変更。**`PLATFORMS`配列自体（6モール分の
  key/label定義）は変更していない**——AIリサーチ機能（新商品の売価想定チェック等）は
  引き続き6モール全部を対象にしており、`PLATFORMS`はその機能とも共有されているため
  （価格アラートの依頼書き換えは価格アラート機能のみのスコープ）。設定画面
  （`#pwMallChecks`）のチェックボックス自体も6モール分残しているので、必要になれば
  いつでも他のモールを手動で有効化できる。
- **既存データの一度だけの移行**：`state.priceWatch.mallsMigratedToDefault`フラグで
  1回だけ判定し、保存されている`malls`が旧デフォルト（6モール全部、＝一度も設定画面で
  選び直していない状態）と完全一致する場合のみAmazon・楽天の2つに書き換える
  （`markDirty()`で即保存）。**すでに手動でモールを選び直していたユーザーの設定には
  一切触れない**——「6モール全部＝カスタマイズ未実施の合図」という一度きりのヒューリスティック。
- 依存するテキストを一通り更新：`pwSchemaTemplate()`のmallキー説明、
  `pwBuildResearchPrompt()`/`pwBuildSingleResearchPrompt()`のbot対策注記、登録フォームの
  モール別URL欄の説明文、`#pwCheckInfoPopover`の説明文（ここで「毎朝9時に自動実行される
  ルーティン」という既に古くなっていた記述も、実態＝依頼文末尾の10時自動化提案に合わせて
  修正）、設定画面のモールチェック欄の説明文。**AIリサーチ機能側の6モール前提の文言
  （新商品リサーチの確度チェック等）は今回のスコープ外として変更していない**。

### ⚠ 「毎朝9時に完全自動」の実態（重要な制約）

このアプリはサーバーを持たない単一HTMLなので、**「今すぐチェック」ボタンを押しても
アプリ自身が裏でブラウザを操作することはできません。** AIリサーチ・ニュース機能と全く同じ
「依頼文をコピー→Claudeとの会話に貼り付け→返ってきたJSONを貼り戻す」方式です
（`pwBuildPrompt()` / `#pwResultInput` / `pwApplyResultJson()`）。

- **Claude in Chrome（ブラウザ拡張）は対話セッション中にしか動作せず、無人の早朝に勝手に
  起動することはできません。** 全モールを高精度に確認したい場合は、この方式（この会話や
  類似のインタラクティブセッション）を使う運用にしてください。
- 「毎朝9時」の自動化は、ニュース機能と同様に**クラウド定期実行ルーティン**（`RemoteTrigger`）
  で実現しています。ただしこちらは`WebSearch`/`WebFetch`ツールのみでモールの検索結果URLを
  HTTP取得するだけの仕組みで、**実ブラウザ操作ではありません。**
- **このクラウド定期実行の結果も、アプリへ自動反映はされません。** ニュース機能と同様、
  ルーティンの実行結果セッションを開いてJSONをコピーし、アプリの「価格アラート」→
  「調査結果のJSONを貼り付け」欄に貼る一手間が必要です。
- ルーティン名「毎朝9時 価格アラートモール調査（新商品開発ナビ用）」（`trig_01PARLPMDm9efvC67ju27kc6`、
  毎日9:05 JST）。**プロンプト内に登録商品をハードコードしています**（アプリ側の登録商品
  一覧とは自動連携しません＝サーバーがないため）。実際の監視商品が変わったら、
  `RemoteTrigger action:update` でこのルーティンのプロンプトも手動更新してください。
  **→ 2026年8月27日、`enabled: false` で無効化済み。** 下記「楽天・Amazonは公式APIでの
  自動取得に置き換え済み」のセクションに完全に置き換わったため。無効化した理由は
  プロンプトの陳腐化だけでなく、**このルーティンの実行環境でWebFetchツールがネットワーク
  egressポリシーにより全ドメインへのアクセスを拒否するようになっており**（無関係な
  `example.com` へのWebFetchも `EGRESS_BLOCKED` で失敗することを確認）、そもそも技術的にも
  動かなくなっていたため。RemoteTriggerには削除コマンドが無いため無効化までで留めている。
  完全に削除する場合は https://claude.ai/code/routines から手動操作が必要。
- Claude in Chrome経由でモールを直接自動巡回させる案（AIリサーチ機能の項でも言及）は、
  この価格アラート機能でも同じ理由（無人実行不可）で見送っています。

### ⚠ 上記は2026年8月20日時点の記述。楽天・Amazonは公式APIでの自動取得に置き換え済み（2026年8月26日〜）

**「Amazon・au PAYマーケット・Qoo10はbot対策でデータ不足になる」という上記の記述は、
Amazonについてはもう古い。** `scripts/rakuten_price_check.py` と `scripts/amazon_price_check.py`
を作り、公式API（スクレイピングではない）で実際に競合価格を取得できることを確認済み。

- **楽天市場**：Rakuten Web Service API（`openapi.rakuten.co.jp/ichibams/...`、v20260701）。
  `applicationId` + `accessKey`（クエリパラメータ）+ `Origin`ヘッダーが必須。無料・
  アフィリエイト登録不要。
- **Amazon**：SP-API。`searchCatalogItems`（キーワード→ASIN、「商品の出品」ロール）→
  `getItemOffers`（ASIN→価格、「価格」ロール）の2段構成。自社Seller Centralアカウントへの
  プライベートアプリとして無料で利用（2026年5月に第三者開発者向け年会費$1,400は撤回済み、
  そもそも自社利用は対象外）。Pricing APIのレート制限に注意（呼び出し間隔1.2秒では
  429が発生、2.2秒＋自動リトライで解消）。
- **Yahoo!ショッピング：断念（2026年8月26日、調査の結果）。** Client IDを2回取得したが
  いずれも `403 Your Request was Forbidden`。パラメータ・ヘッダー（User-Agent・Referer）・
  エンドポイント（v1/v2/v3）を変えても症状は一切変わらず、再取得しても同じ結果だった。
  技術的な設定ミスではなく、**Yahoo!の「非商用目的のみ」という基本方針による構造的な
  ブロックの可能性が高い**と判断した。ショッピングAPIには商用利用の相談窓口が公式に
  存在せず（「提供しておりません」と明記）、法人・業務用途として登録したことが
  Yahoo!側で自動的に検知され、承認されないままブロックされていると推測される。
  - **「個人利用」として登録し直す、非商用に見える説明文に変える、といった回避策は
    採用しなかった。** 実際の用途を偽ってYahoo!の利用規約に反する形でアクセス制限を
    回避することになるため。
  - ユーザー判断で「ここで断念する」方針を確定。Yahoo!は他の3モール
    （au PAYマーケット・Qoo10・カウシェ）と同様、引き続き手動チェックの対象とする。
  - もし再挑戦する場合は、Yahoo!（LYコーポレーション）の一般問い合わせ窓口（開発者
    ポータルの窓口ではない）に正式に商用利用の可否を問い合わせるのが唯一の正攻法。
- **au PAYマーケット・Qoo10・カウシェ**：キーワード検索の公式APIなし。バリューコマース
  （ASP）経由の可能性はあるが、アフィリエイト審査が必要で用途がズレるため未着手。
  現状は引き続き手動チェック。

**いずれも「判定はせず価格一覧を届ける」方式**（🔴🟢等の自動判定はしない）。
実データ検証で、キーワード検索は生鮮食品と相性が悪く誤判定を招くと分かったため
（例：「とうもろこし」でエプソン互換インクが競合最安値として混入した実例あり）、
`--json` 出力をアプリに貼り付けた後の最終判断は人が行う設計にしている。
詳細は `scripts/README.md` を参照。

**2026年8月26日：毎朝9時の自動実行を設定済み。** Windowsのタスクスケジューラに
`新商品開発ナビ_価格レポート` というタスクを登録し、`scripts/daily_price_check.py`
（楽天・Amazonをまとめて実行し1つのJSONに統合するラッパー）を毎朝9時に無人実行する。

**方式の選択について**：クラウド定期実行（`RemoteTrigger`）ではなく、あえて
**ローカルのタスクスケジューラ**を選んだ。理由は、クラウド定期実行だとAmazonの
`Refresh Token`（強い権限を持つ機微な鍵）をクラウド側のルーティン設定に登録する
必要があり、「鍵はローカルの非公開フォルダにしか置かない」という一貫した方針から
外れるため。トレードオフとして、**会社のPCが朝9時に起動していないと実行されない**。

- 出力は `_ローカル専用（共有しない）/価格レポート/最新.json`（アプリ貼り付け用）と
  `最新.txt`（人が読む用）、`実行ログ.txt`（成功/失敗の記録）。詳細は`scripts/README.md`参照。
- **アプリへの反映はまだ自動化されていない。** 毎朝ファイルは生成されるが、
  「価格アラート」→「調査結果のJSONを貼り付け」への貼り付けは引き続き手動。
- タスク登録はOS側の設定のため、このリポジトリには含まれない。別のPCで同じ自動実行を
  したい場合は、`scripts/README.md`の手順で登録し直す必要がある。

## 広告シミュレーター機能（親ASIN単位の黒字転換予測）（2026年8月28日〜）

上長への提案資料用に、**特定の親ASIN（ポートフォリオ）に投じた初期広告費がいつ
黒字転換（黒転）するか**を週次52週で試算する画面。事業全体ではなく対象ポートフォリオの
収益化だけにフォーカスしている。下部ナビの「📈 広告シミュ」から開く。

### ⚠ AIリサーチをナビから外した件（**セクション本体は絶対に消さないこと**）

ユーザー依頼でボトムナビとホームタイルから「AIリサーチ」を外したが、
**`<section id="view-airesearch">` とその中のDOM・`air*`系JSは無傷で残している**。
理由：`renderAll()` が起動時に `airName` / `airVolume` / `airCategory` / `airKeyword` /
`airGrade` / `airPlatformChecks` / `airReport` を**無条件に** `getElementById` しており、
`renderOutputView()` も `summaryPreview2` を触る。これらは全て当該セクション内にあるため、
**セクションを削除すると起動時に例外が出てアプリ全体が動かなくなる**。
`.topView` は `display:none` するだけでDOMには残るので、**到達経路（ナビのボタンと
ホームタイル）だけをHTMLコメントで塞ぐのが唯一安全な方法**。
`VIEW_IDS` から `"airesearch"` を消すのも不可（履歴モーダルの「この結果を読み込む」が
`goToView("airesearch")` を呼ぶ）。実機で「履歴→AIリサーチ→読み込む」が従来どおり
動作することを確認済み。復活させたい場合は2箇所のコメントを外すだけでよい。

### 採用しなかった案：Recharts

ユーザーの当初指定はRecharts（React専用ライブラリ）だったが、**このアプリは
`<script src>` も `<link>` も1つ持たない完全な単一HTML**。React+ReactDOM+Rechartsで
50万バイト級のCDN依存が増え、社内回線でCDNが遮断されるとグラフが壊れる。
ユーザー確認の上、既存 `renderAirChart()` と同じ「SVG文字列を組み立てて innerHTML」
idiom で自作した（色は `var(--series-N)` 参照なのでダークモードに自動追従、
ホバーは共通の `#tooltip` を流用）。

### データモデル `state.adSim`

```
portfolios[] : { id, parentAsin, name, consultPct,
                 children[]:{id,sku,cost,feeYen,sharePct},   // 金額は1個あたりの「円」固定（％不可）
                 monthly[13]:{adSpend,cpc,cvr,aov,organic},  // 4週×13ブロック=52週
                 weekOverrides:{ "7":{...} },                // 週次の個別上書き
                 scenarios:{bull,base,bear} }                // 標準広告費に対する倍率(%)
selectedPortfolioId, compareMode, lastResult
```

永続化は `priceWatch` と同じ6点セット（初期state / `adSimEnsureState()` /
`syncBuildPayload()` / `syncApplyRemote()` / `#importFile` / `markDirty()`）。
**`syncBuildPayload()` に入れ忘れるとリロードで消える**（localStorage保存はこの戻り値のみ）。

### 週次上書きは「週まるごとピン留め」

仕様の「上書きした週は後から月次を変えても影響を受けない」を曖昧さなく満たすため、
**どれか1項目でも編集したらその週の実効値5項目すべてをスナップショット**して
`weekOverrides` に固定する（部分上書きにすると「CPCだけ編集した週の広告費が
月次変更で動く」という分かりにくい挙動になる）。行に📌が付き「月次に戻す」で解除。
月次を編集したときは、**未ピンの週の入力欄の表示値だけ**を書き換える
（`adSimSyncWeeklyDisplay`）。行ごと作り直さないので編集中のフォーカスが飛ばない。

### 計算ロジックの注意点

- `clicks = 週間広告費 / 想定CPC` は**機械的な簡易モデル**で、Amazon管理画面の
  セッション数とは異なる。コード内にもコメントで明記済み。
- 自然流入は「割合」ではなく**週間自然流入売上として直接加算**。ゼロ除算を避けるため。
- 販売個数 `units = 子ASIN別売上 / 想定客単価` は**端数を丸めない**。
  52週積み上げると丸め誤差が無視できない大きさになるため。
- シナリオ倍率は**週間広告費にのみ**掛かる。①②の他の設定は3シナリオ共通。

### バリデーションとエラー時の3状態

- 売上占有率は合計100.0%必須。**判定と表示のときだけ小数第1位に丸める**
  （内部値は丸めない）。これがないと `33.3+33.3+33.4` が浮動小数点誤差で弾かれる。
  自動調整はせず「◯%不足／◯%超過」を赤字表示。
- エラーが1件でもあれば**「計算実行」「シナリオ保存」を両方 `disabled`**。
- グラフは3状態：①未計算＝空枠に「パラメータを入力してください」
  ②正常＝描画 ③**エラー＝直前の結果を凍結表示**したうえで半透明オーバーレイ＋
  「入力エラーを解消してください」、さらに黒転テキストをグレーアウトして
  「（※直前の入力に基づく参考値）」を併記。
- 月次由来のエラーはブロックにつき1回だけ報告する（同じ内容が4週分×4回出ないように）。

### 再描画の方針（フォーカス飛び対策）

**値の入力中はテーブルを再描画しない。** `input` ではバリデーション表示とボタンの
`disabled` だけ更新し、テーブルの `innerHTML` 再構築は構造が変わったとき
（ポートフォリオ切替・行の追加削除・ピン留め/解除）に限定している。
再計算とグラフ描画は「計算実行」ボタン（クリック起点）なのでタイピング中に走らない。

### 既知の小さな落とし穴

- 既存の `attr()` は `attr(0)` が `""` を返す（`v||""` のため）。数値を value 属性に
  入れるときは `attr(String(n))` を通すこと。通さないと**0の項目が空欄で描画される**。
- CSS変数は `:root` ではなく **`.viz-root`** に定義されている。新しいマークアップは
  `.viz-root` 配下（`<div class="app">`）に置かないと `var(--...)` が解決しない。
- 既存JSは徹底してES5スタイル（`var`/`function`、アロー関数もテンプレートリテラルも
  1つも使われていない）。新規コードも揃えてある。

## ニュース機能

- 下部ナビの「ニュース」から、ニュースの要約を日経新聞風レイアウトで表示します
  （`.newsPaper` 関連CSS、`buildNewsPaperHtml(data, kind)`）。
- 貼り付けるJSONの `updatedAt`（日付）ごとに1レコードとして保存し、日付タブで
  過去分を切り替えて閲覧できます。同じ日付を再度貼り付けると上書き。
- **毎朝9時（日本時間）に自動でニュース調査を行うスケジュールタスクを、開発時のClaude
  アカウント側で `create_trigger` により作成済みです。これは元のアカウントに紐づいており、
  別アカウントには自動で引き継がれません。** 新しい環境で同じ自動配信が欲しい場合は、
  トリガーのプロンプト内容（本ファイルと同じリポジトリのコミット履歴、または元の会話ログに
  記載）を参考に、新しいアカウント側で `create_trigger` を作り直す必要があります。
  プロンプトの要点：食品業界ニュースをWeb検索→growingGenres/decliningGenres/majorNewsの
  3分類でJSON化→ユーザーにチャットで送る、という内容です。**この自動配信は「食品業界ニュース」
  カテゴリのみを対象にしています**（下記のAIニュース追加時点では未対応）。

### 2026年8月27日：「食品業界ニュース」「AIニュース」の2カテゴリに拡張

トップ画面・履歴モーダルの両方で、2つのボタン（`.newsCategoryTab`、data-cat="food"/"ai"）
により切り替えられるようにした。データ・ロジックは完全に独立した2系統で、`NEWS_CATEGORY`
オブジェクト（`historyKey`/`dateKey`/`title`/`hint`/`placeholder`/`hasContent`/`invalidMsg`
をカテゴリごとに1箇所にまとめたもの）で分岐先を一元管理している。

- **食品業界ニュース**：`state.newsHistory[]`。既存のスキーマ（`growingGenres`/
  `decliningGenres`/`majorNews`）は変更していない。
- **AIニュース**：`state.aiNewsHistory[]`。新規スキーマ（`vendorNews`/`claudeTips`/
  `todayExample`）。
  - **食品の「ジャンルの成長/衰退」という枠組みは、AI業界のニュース（モデル発表・企業動向
    など個別ニュース中心）には合わないと判断し、あえて異なるスキーマにした**（ユーザーとの
    合意の上での設計判断）。
  - `vendorNews`は`vendor`フィールド（"Claude"/"ChatGPT"/"Gemini"等）を持つニュースの
    フラット配列。表示時に`vendor==="Claude"`かどうかで「Claudeの動向」（深掘り・専用
    セクション）と「各社の動向」（その他ベンダーをまとめて表示）に振り分ける
    （`buildNewsPaperHtml`内、`kind==="ai"`の分岐）。Claudeを特に厚く扱ってほしいという
    ユーザー要望を反映した構成。
  - `claudeTips`・`todayExample`はAI業界ニュース特有の項目（Claudeの実践活用法、
    実際の活用事例の紹介。特にX(旧Twitter)で話題になった投稿を紹介する記事を優先的に
    検索するよう`buildNewsPrompt("ai")`の指示文に明記している）。
- 紙面レンダリングの共通部分（1件の記事カードのHTML）は`paperArticleHtml()`として
  切り出し、食品の`majorNews`・AIの`vendorNews`/`todayExample`のいずれからも再利用
  している（丸ごと複製していない）。
- 履歴モーダルのニュースタブ（`renderHistoryNewsTab`）にも同じカテゴリ切替を実装。
  トップ画面と`state.ui.activeNewsCategory`を共有しているため、どちらで切り替えても
  もう一方に反映される。
- 同期・エクスポート・インポート（`syncBuildPayload`/`syncApplyRemote`/`#importFile`）に
  `aiNewsHistory`を配線済み。既存の「配列でなければ空配列にフォールバック」パターンを踏襲。

## クラウド同期機能（SharePoint連携）

- 設定画面の「☁ クラウド同期」から、Power AutomateのHTTPフロー経由でSharePoint上の
  JSONファイルと同期できます（詳細手順は別途お渡し済みの構築手順書を参照）。
- **通信は `Content-Type: text/plain` のPOSTのみ**（カスタムヘッダーなし）。これは
  CORSプリフライト（OPTIONS）を避けるためで、Power AutomateのHTTPトリガーは
  OPTIONSに正しく応答しないことが多く、ヘッダー付きだと原因不明の接続失敗になります。
  この設計は変えないことを推奨します。
- 本文の `action`（`"load"` or `"save"`）で1本のフローが両方の処理を分岐します。
- 同期間隔は既定30秒（最短10秒）。5秒間隔などにするとPower Automateの1日あたり
  APIリクエスト上限に複数人利用で容易に達するため、既定値を変えないよう案内しています。
- タブが非表示のときは通信しない、編集は1.5秒デバウンスしてから1回だけ保存、
  タブ復帰時は即座に取得、というポーリング節約の仕組みが入っています（`sync` オブジェクト）。
- フローURL・トークンはアプリのソースには一切埋め込まれておらず、利用者が設定画面で
  入力してその端末の `localStorage` にだけ保存されます（GitHub Pagesは公開されるため、
  埋め込むとトークン漏洩になる）。

### ⚠ 2026年8月26日時点：構築を保留中（Power Automate Premiumライセンスが必要）

「他のPCにもデータを引き継ぎたい」という要望を受け、Power AutomateでSharePoint連携
フロー（`新商品開発ナビ_データ同期`）を実際に組み上げた。トリガー・本文解析・
トークン確認・load/save分岐・SharePointの読み書きまで、すべてのアクションは
完成している。

**しかし保存時に「Your flow is saved but can't be used」というエラーが出た。**
原因は「Response」アクション（3箇所すべて）が、この会社のテナントでは
**Power Automate Premiumライセンスが必要な扱いになっている**ため
（本来は無料枠のはずの`Request`コネクタだが、テナントの契約プランによってはPremium
判定になることがある）。

- 個人の判断で90日間無料トライアルを開始することは避け、ユーザー判断で保留を選択。
  理由：トライアル終了後に自動課金へ移行するリスクがあり、会社のMicrosoft契約に
  関わる決定を一開発者の判断だけで行うべきではないため。
- **フロー自体はPower Automate上に保存済みで消えていない。** ライセンスが確保でき
  次第、フローを開いてそのまま「Save」し直せば使えるようになる見込み（設計・実装は
  完了しており、あとはライセンスの問題だけ）。
- サイトは「マーケティング部」チームのSharePoint
  （`https://cms00moriuchi.sharepoint.com/sites/msteams_dc861a`）、ファイルは
  `Shared Documents/shinshouhin-navi-data.json`。
- 再開する場合は、社内の情報システム部門・Microsoft契約管理者にPremiumライセンスの
  要否を確認してから、フローを開いて保存し直す、という流れになる。

## 開発・検証の進め方（踏襲を推奨）

これまでの開発では、変更のたびに次の手順で検証してから納品しています。

1. `<script>` タグの中身をPythonで抽出し `node --check` で構文エラーがないか確認
2. Playwright（`chromium` = `/opt/pw-browsers/chromium`）でheadlessブラウザテストを書き、
   DOM状態を実際にアサートして確認（`page.on('pageerror', ...)` でJSエラーも収集）
3. スクラッチのテストファイル・スクリーンショットは検証後に `rm -f` で削除

この積み重ねで、上記の「ログインが効かなくなる」系のバグを含め、いくつかの実環境依存の
不具合を事前に潰しています。新しい機能を足す際も同様の検証を挟むことを推奨します。

## パスワード・トークンの管理

**このリポジトリは GitHub で公開されているため、パスワードやトークンをこのファイルに書いてはいけません。**

以前はこのセクションにログインパスワードと SharePoint 同期トークンを平文で記載していましたが、
公開リポジトリに機密情報を置いている状態だったため削除しました（2026年8月20日）。
**Git の履歴には旧い値が残っているため、旧い値は今後一切使わないでください。**

対応状況：

| 項目 | 対応 | 保管場所 |
|---|---|---|
| アプリのログインパスワード | **値を変更済み**（旧 `local123` は無効） | `_ローカル専用（共有しない）/`（`.gitignore` で除外済み） |
| SharePoint 同期用トークン | **未変更**。クラウド同期は未設定のまま運用しているため、変更対象なし。今後同期を使い始めるときは、記載されていた旧トークンは使わず新しい値を発行すること | 使い始めたら同上 |
| 各モールAPIの鍵（楽天・Yahoo!・Amazon） | 取得したら記録する | 同上 |

パスワードを変更したい場合は、`_ローカル専用（共有しない）/パスワード変更ツール.html`
をブラウザで開き、新しいパスワードを入力して出てくるハッシュ値を
`shinshouhin-navi.html` の `STORED_HASH` に設定してください
（このツールはローカルで動くだけで、パスワードはどこにも送信されません）。

なお、ログインの仕組みは cyrb53 という非暗号学的ハッシュをクライアント側で照合しているだけなので、
**本物のセキュリティにはなりません**（ソースを読めば `STORED_HASH` は見えるし、総当たりも容易）。
社内の軽い間仕切りとしてのみ機能する、という前提は変わっていません。

以上です。不明点があれば、GitHubのコミットメッセージと合わせてこのメモを参照してください。
